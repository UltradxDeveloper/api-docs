## 鉴权接口

这里是代码：`/open/v1/balance`

```javascript
bindpass() {
  if (this.props.userAuth.isGoogleAuth === true) {
    this.props.history.push('/comm/security/bindfundpassword')
  } else {
    Message.error(t('safe.bindGoogleFirst'))
  }
}
// 修改资金密码
changepass() {
  if (this.props.userAuth.isGoogleAuth === true) {
    this.props.history.push('/comm/security/changefundpassword')
  } else {
    Message.error(t('safe.bindGoogleFirst'))
  }
}2
```

